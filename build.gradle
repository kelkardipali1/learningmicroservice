plugins {
	id 'org.springframework.boot' version '2.1.16.RELEASE'
	id 'io.spring.dependency-management' version '1.0.9.RELEASE'
	id 'java'
	id 'maven-publish'
}

group = 'com.thoughtworks'
version = '0.0.1-SNAPSHOT'
sourceCompatibility = '1.8'

repositories {
	mavenCentral()
}

dependencies {
	implementation 'org.springframework.boot:spring-boot-starter-web'
	runtimeOnly 'com.h2database:h2'
	testImplementation 'org.springframework.boot:spring-boot-starter-test'
}

publishing {
	publications {
		github(MavenPublication) {
			groupId = 'com.thoughtworks'
			artifactId = 'democlient'
			version = '1.1.1'
			from(components.java)
		}
	}
	repositories {
		maven {
			name = "demo"
			url = uri("https://maven.pkg.github.com/kelkardipali1/demo")
			credentials {
				username = "kelkardipali1"
				password = "79e8fded83bb409cd556690163f88d246ef0dcf4"
			}
		}
	}
}
